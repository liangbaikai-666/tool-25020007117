```bash

```

# q01 习题解答

## 1. ls -l 选项；ls -l / 首行 10 字符含义

**作用**：`ls -l`：以长格式列出文件 / 目录详细元数据。

运行命令：

bash

```
ls -l /
```

首行 10 个字符含义：

格式：`[type][rwxrwxrwx]`

- 第 1 位：文件类型：`-`普通文件，`d`目录，`l`符号链接，`b/c`块 / 字符设备
- 2‑4 位：所有者 (user) 权限 r w x
- 5‑7 位：所属组 (group) 权限 r w x
- 8‑10 位：其他用户 (other) 权限 r w x

## 2. glob 概念；glob 模式实验

**glob（通配模式）**：Bash shell 在命令执行前对文件名做的模式展开，shell 把`* ? {}`等匹配符替换成真实匹配到的文件名；这属于 shell 处理，不是程序本身功能。

测试命令集合：

bash

```
# 新建测试目录
mkdir -p ~/glob_test && cd ~/glob_test
# 创建测试文件
touch a.txt b.txt c.txt file1.txt file2.txt fileX.txt
ls *.txt          # * 匹配任意长度任意字符
ls file?.txt      # ? 匹配恰好一个任意字符
ls {a,b,c}.txt    # {a,b,c}集合展开，匹配a.txt b.txt c.txt
cd -
```

- `*`：匹配 0 或多个任意字符
- `?`：匹配恰好 1 个字符
- `{x,y,z}`：集合展开，生成多个文件名

> 原题命令：`find ~/Downloads -type f -name "*.zip"`，`-name`的参数是传给 find 程序，find 内部自己处理通配符，此时不是 shell glob 展开。
>
> 

## 3. 单引号、双引号、\(' ' ANSI引号区别；输出含 `\)!` 换行字符串

表格

| 引号               | 行为                                                         |
| ------------------ | ------------------------------------------------------------ |
| `' '` 单引号       | 完全字面，所有字符原样输出，无任何转义、变量替换             |
| `" "` 双引号       | 保留字面，但允许 `$`变量替换、`\`转义、`!`历史扩展 (部分环境) |
| `$' '` ANSI‑C 引号 | 支持 `\n \t \r` 等 C 风格转义序列，实现换行、制表符          |

输出同时包含字面`$`、`!`、换行符的命令：

bash

```
echo $'literal dollar: $ \nliteral exclamation: !'
```

## 4. 标准流 stdin (0) stdout (1) stderr (2)；重定向

运行会同时产生正常输出 + 错误输出：

bash

```
ls /nonexistent /tmp 1>stdout.txt 2>stderr.txt
```

- `1>stdout.txt`：stdout (1) 重定向到 stdout.txt
- `2>stderr.txt`：stderr (2) 重定向到 stderr.txt

stdout、stderr 合并到同一个文件两种写法：

bash

```
# 写法1（推荐bash）
ls /nonexistent /tmp &> all.txt

# 写法2 POSIX兼容
ls /nonexistent /tmp > all.txt 2>&1
```

## 5. $? 退出状态，&& ||；仅当目录不存在才创建 /tmp/mydir

逻辑：`[ ! -d "/tmp/mydir" ]` 判断目录不存在，成立才执行 mkdir

一行命令：

bash

```
[ ! -d "/tmp/mydir" ] && mkdir /tmp/mydir
```

- `$?`：上一条命令返回值，0 = 成功；非 0 = 失败。
- `&&`：前面返回 0 才执行后面；`||`：前面非 0 才执行后面。

## 6. 为什么 cd 必须是 shell 内置命令，不能外部独立程序？

进程工作原理：外部命令会创建子进程。

1. 每个进程拥有自己独立的工作目录 (cwd)。
2. 子进程修改自己的工作目录，完全不会影响父 shell 进程的当前目录。
3. 如果 cd 是外部程序，运行结束子进程退出，父 shell 的工作目录毫无变化，cd 就完全失效。
4. 作为 shell 内建命令，直接在 shell（父进程）内部修改自身的 cwd，才能真正切换 shell 当前目录。

## 7. Shell 脚本：接收 $1 参数，判断文件是否存在

bash

```
#!/bin/bash
# $1 接收传入的文件名参数
if [ -f "$1" ]; then
    echo "文件 $1 存在，是普通文件"
else
    echo "文件 $1 不存在或不是普通文件"
fi
```

终端测试示例：

bash

```
bash check.sh /etc/passwd
bash check.sh /does_not_exist
```

## 8. chmod +x 的作用；./check.sh 报错原因

直接运行 `./check.sh somefile`：会报错 `Permission denied`，没有执行权限。

执行：`chmod +x check.sh`，给文件增加执行权限，之后 `./check.sh somefile` 正常运行。

**原因：**

- `ls -l check.sh` 看权限位，chmod 前缺少`x`执行位。
- `./xxx` 这种直接路径运行方式，操作系统要求文件具备执行权限。
- 用 `bash check.sh` 不需要 x 权限：此时 bash 作为解释器读取文本，不是把脚本当作可执行程序直接运行。

bash

```
ls -l check.sh
chmod +x check.sh
ls -l check.sh
```

## 9. set -x 的作用

`set -x`：开启调试跟踪模式；shell 在执行每一行前，打印展开后的命令到 stderr，方便排错。

测试脚本 `debug_demo.sh`

bash

```
#!/bin/bash
set -x
filename="$1"
echo "输入参数是 $filename"
ls -l "$filename"
set +x   # set +x 关闭调试
echo "调试结束"
```

运行：

bash

```
chmod +x debug_demo.sh
./debug_demo.sh /tmp
```

> 输出会以 `+` 开头打印每一条实际执行的命令。
>
> 

## 10. Command Substitution：复制文件附加当天日期备份

`$(date +%Y-%m-%d)` 命令替换，把 date 命令输出嵌入字符串。



bash

```
cp notes.txt "notes_$(date +%Y-%m-%d).txt"
```

测试：

bash

```
echo test > notes.txt
cp notes.txt "notes_$(date +%Y-%m-%d).txt"
ls notes*.txt
```